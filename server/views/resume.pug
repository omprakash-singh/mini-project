doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Document
  script(src='https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.4/jspdf.debug.js')
  style.
    .main-container {
    line-height: 5px;
    }
    h1 {
    font-size: 30px;
    text-transform: uppercase;
    font-size: 28px;
    }
    h3 {
    text-transform: uppercase;
    font-size: 15px;
    }
    h4 {
    font-size: 20px;
    }
  // link here css file
  link(rel='stylesheet' href=`CSS/${SetCSS}.css`)
#test.container.main-container
  .template--name
    h1= `${data[0].fname} ${data[0].lname}`
  .template--info
    h3 info
    .container
      h4
        label(for='address')= `Address: ${data[0].address}`
      h4
        -if(data[0].phone != undefined)
          label(for='contact')= `Contact: ${data[0].phone}`
        | &nbsp;&nbsp;&nbsp;
        label(for='address')= `Email: ${data[0].email}`
  .skill
    h3 Skill
    .container
      h4
        - if(data[0].skill1 != "")
          label(for='skill1')= `Skill: ${data[0].skill1}`
          |  &nbsp;&nbsp;&nbsp;
          label(for='Level')= `Level: ${data[0].skill1level}`
      h4
        - if(data[0].skill1 != "")
          label(for='skill1')= `Skill: ${data[0].skill1}`
          |  &nbsp;&nbsp;&nbsp;
          label(for='Level')= `Level: ${data[0].skill1level}`
      h4
        - if(data[0].skill1 != "")
          label(for='skill1')= `Skill: ${data[0].skill1}`
          |  &nbsp;&nbsp;&nbsp;
          label(for='Level')= `Level: ${data[0].skill1level}`
      h4
        - if(data[0].skill1 != "")
          label(for='skill1')= `Skill: ${data[0].skill1}`
          |  &nbsp;&nbsp;&nbsp;
          label(for='Level')= `Level: ${data[0].skill1level}`
      h4
        - if(data[0].skill1 != "")
          label(for='skill1')= `Skill: ${data[0].skill1}`
          |  &nbsp;&nbsp;&nbsp;
          label(for='Level')= `Level: ${data[0].skill1level}`
  .summmary
    h3 Summary
    p= data[0].summary
  .employment--history
    h3 employment
    .container
      h4
        label(for='Employer')= `Employer: ${data[0].employer}`
        | &nbsp;&nbsp;&nbsp;
        label(for='Jobtitle')= `Job title: ${data[0].jobtitle}`
      h4
        label(for='city')= `City: ${data[0].jobcity}`
        | &nbsp;&nbsp;&nbsp;
        label(for='state')= `State: ${data[0].jobstate}`
      h4
        label(for='startdate')= `Start date: ${data[0].jobstartdate}`
        | &nbsp;&nbsp;&nbsp;
        label(for='endDate')=  `End Date: ${data[0].jobenddate}`
  .education
    h3 EDUCATION
    .container
      h4
        label(for='Schoolname')= `School name: ${data[0].schoolname}`
        |   &nbsp;&nbsp;&nbsp;
        label(for='City')= `City: ${data[0].schoolcity}`
      h4
        label(for='State')= `State: ${data[0].schoolstate}`
        |  &nbsp;&nbsp;&nbsp;
        label(for='Degreee')= `Degree: ${data[0].schooldegree}`
      h4
        label(for='Fieldofstudy')= `Field of study: ${data[0].fieldofstudy}`
        |  &nbsp;&nbsp;&nbsp;
        label(for='Graduatiodate')=  `Graduation date: ${data[0].gdate}`

input#printpagebutton(onclick='printpage()' type='button' value='Print Resume')
script.
  function printpage() {
  var printButton = document.getElementById("printpagebutton");
  printButton.style.visibility = 'hidden';
  window.print()
  printButton.style.visibility = 'visible';
  }
button(onclick='generatePDF()') DownLoad PDF
script.
  function generatePDF() {
  var doc = new jsPDF();  
  doc.fromHTML(document.getElementById("test"), 
  15,
  15,
  {
  'width': 170  
  },
  function (a) {
  doc.save("HTML2PDF.pdf"); 
  });
  }